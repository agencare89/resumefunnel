<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Resume Funnel</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-left">
        <li class="<%= typeof index != 'undefined' ? index : '' %>"><%- link_to("Job Postings", "/") %></li>
        <% if (user) { %>
          <li class="<%= typeof dashboard != 'undefined' ? dashboard : '' %>"><%- link_to("Dashboard", "/dashboard") %></li>
        <% } %>
      </ul>
      <div class="nav navbar-nav navbar-right">
        <% if (user) { %>
          <span class="right-margin">Welcome back, <%= user %></span>
          <a href="/login" class="btn btn-default navbar-btn" role="button">Logout</a>
        <% } else { %>
          <a href="/login" class="btn btn-default navbar-btn" role="button">Login <span class="glyphicon glyphicon-log-in" aria-hidden="true"></span></a>
          <a href="/register" class="btn btn-primary navbar-btn" role="button">Register</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>